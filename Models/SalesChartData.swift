//
// SalesChartData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Weekly aggregated data optimized for React shadcn charts (recharts) */
public struct SalesChartData: Sendable, Codable, Hashable {

    /** Week start date in ISO 8601 format (YYYY-MM-DD), Sunday as week start */
    public var date: String
    /** Net sales for this week (Gross − Discounts − Refunds) */
    public var netSales: Double
    /** Gross sales (subtotals) for this week */
    public var grossSales: Double
    /** Total discounts for this week */
    public var discounts: Double
    /** Net tax collected for this week */
    public var tax: Double
    /** Refunds processed in this week (by refund date) */
    public var refunds: Double
    /** Number of completed orders in this week */
    public var orders: Int

    public init(date: String, netSales: Double, grossSales: Double, discounts: Double, tax: Double, refunds: Double, orders: Int) {
        self.date = date
        self.netSales = netSales
        self.grossSales = grossSales
        self.discounts = discounts
        self.tax = tax
        self.refunds = refunds
        self.orders = orders
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case date = "Date"
        case netSales = "NetSales"
        case grossSales = "GrossSales"
        case discounts = "Discounts"
        case tax = "Tax"
        case refunds = "Refunds"
        case orders = "Orders"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(date, forKey: .date)
        try container.encode(netSales, forKey: .netSales)
        try container.encode(grossSales, forKey: .grossSales)
        try container.encode(discounts, forKey: .discounts)
        try container.encode(tax, forKey: .tax)
        try container.encode(refunds, forKey: .refunds)
        try container.encode(orders, forKey: .orders)
    }
}

