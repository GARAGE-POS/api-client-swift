//
// GenerateDownloadSasTokenRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Request to generate a SAS token for file download */
public struct GenerateDownloadSasTokenRequest: Sendable, Codable, Hashable {

    public static let containerNameRule = StringRule(minLength: 0, maxLength: 63, pattern: "/^[a-z0-9](?:[a-z0-9\\-]*[a-z0-9])?$/")
    public static let blobNameRule = StringRule(minLength: 0, maxLength: 1024, pattern: nil)
    public static let expirationMinutesRule = NumericRule<Int>(minimum: 5, exclusiveMinimum: false, maximum: 1440, exclusiveMaximum: false, multipleOf: nil)
    /** Optional container name. If not specified, generates an account-level SAS token that allows downloading from all containers. If specified without BlobName, generates a container-level SAS token for that specific container. If specified with BlobName, generates a blob-level SAS token for that specific file. Example: \"invoices\", \"receipts\", \"documents\" */
    public var containerName: String?
    /** Optional blob name (file path) of the file to download. If provided with ContainerName, generates a blob-level SAS for that specific file. If not provided with ContainerName, generates a container-level SAS. If neither is provided, generates an account-level SAS. Example: \"2024/11/invoice-12345_ab99f957.pdf\" */
    public var blobName: String?
    /** Optional expiration time in minutes for the SAS token. If not specified, defaults to 60 minutes. Must be between 5 and 1440 minutes (24 hours). Example: 60, 120, 1440 */
    public var expirationMinutes: Int?

    public init(containerName: String? = nil, blobName: String? = nil, expirationMinutes: Int? = nil) {
        self.containerName = containerName
        self.blobName = blobName
        self.expirationMinutes = expirationMinutes
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case containerName = "ContainerName"
        case blobName = "BlobName"
        case expirationMinutes = "ExpirationMinutes"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(containerName, forKey: .containerName)
        try container.encodeIfPresent(blobName, forKey: .blobName)
        try container.encodeIfPresent(expirationMinutes, forKey: .expirationMinutes)
    }
}

