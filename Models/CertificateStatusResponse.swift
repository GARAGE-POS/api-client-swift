//
// CertificateStatusResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Response for certificate status query. */
public struct CertificateStatusResponse: Sendable, Codable, Hashable {

    /** The Terminal ID. */
    public var terminalID: Int64
    /** Whether the terminal has an active certificate. */
    public var hasActiveCertificate: Bool
    public var activeCertificate: CertificateInfo?
    /** List of all certificates for this terminal. */
    public var certificateHistory: [CertificateInfo]

    public init(terminalID: Int64, hasActiveCertificate: Bool, activeCertificate: CertificateInfo? = nil, certificateHistory: [CertificateInfo]) {
        self.terminalID = terminalID
        self.hasActiveCertificate = hasActiveCertificate
        self.activeCertificate = activeCertificate
        self.certificateHistory = certificateHistory
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case terminalID = "TerminalID"
        case hasActiveCertificate = "HasActiveCertificate"
        case activeCertificate = "ActiveCertificate"
        case certificateHistory = "CertificateHistory"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(terminalID, forKey: .terminalID)
        try container.encode(hasActiveCertificate, forKey: .hasActiveCertificate)
        try container.encodeIfPresent(activeCertificate, forKey: .activeCertificate)
        try container.encode(certificateHistory, forKey: .certificateHistory)
    }
}

