//
// SalesReportDto.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Daily aggregated sales row in the report */
public struct SalesReportDto: Sendable, Codable, Hashable {

    /** Date in ISO 8601 format (YYYY-MM-DD) */
    public var date: String
    /** Gross sales — sum of order subtotals (before discounts) for completed/paid orders */
    public var grossSales: Double
    /** Total discounts applied (line item + order-level discounts) */
    public var discounts: Double
    /** Net tax collected (gross tax minus refunded tax amounts for refunds processed on this date) */
    public var tax: Double
    /** Refunds processed on this date (by refund transaction date, not original order date) */
    public var refunds: Double
    /** Net sales = Gross Sales − Discounts − Refunds */
    public var netSales: Double
    /** Number of completed/paid orders on this date */
    public var orders: Int

    public init(date: String, grossSales: Double, discounts: Double, tax: Double, refunds: Double, netSales: Double, orders: Int) {
        self.date = date
        self.grossSales = grossSales
        self.discounts = discounts
        self.tax = tax
        self.refunds = refunds
        self.netSales = netSales
        self.orders = orders
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case date = "Date"
        case grossSales = "GrossSales"
        case discounts = "Discounts"
        case tax = "Tax"
        case refunds = "Refunds"
        case netSales = "NetSales"
        case orders = "Orders"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(date, forKey: .date)
        try container.encode(grossSales, forKey: .grossSales)
        try container.encode(discounts, forKey: .discounts)
        try container.encode(tax, forKey: .tax)
        try container.encode(refunds, forKey: .refunds)
        try container.encode(netSales, forKey: .netSales)
        try container.encode(orders, forKey: .orders)
    }
}

