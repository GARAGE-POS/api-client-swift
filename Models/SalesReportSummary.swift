//
// SalesReportSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Summary/aggregate KPI data for the sales report with weekly chart data */
public struct SalesReportSummary: Sendable, Codable, Hashable {

    /** Total gross sales (sum of subtotals for all completed/paid orders in period) */
    public var totalGrossSales: Double
    /** Total net sales = Gross Sales − Discounts − Refunds */
    public var totalNetSales: Double
    /** Total tax collected (net of refunded tax amounts within the period) */
    public var totalTax: Double
    /** Total discounts applied across all orders in the period */
    public var totalDiscounts: Double
    /** Total refunds issued in the period (by refund transaction date) */
    public var totalRefunds: Double
    /** Total number of completed/paid orders in the period */
    public var totalOrders: Int
    /** Number of voided orders in the period (excluded from Gross/Net Sales) */
    public var totalVoids: Int
    /** Total value of voided orders in the period */
    public var totalVoidValue: Double
    /** Weekly aggregated data for the Net Sales chart (React shadcn/recharts compatible) */
    public var chartData: [SalesChartData]

    public init(totalGrossSales: Double, totalNetSales: Double, totalTax: Double, totalDiscounts: Double, totalRefunds: Double, totalOrders: Int, totalVoids: Int, totalVoidValue: Double, chartData: [SalesChartData]) {
        self.totalGrossSales = totalGrossSales
        self.totalNetSales = totalNetSales
        self.totalTax = totalTax
        self.totalDiscounts = totalDiscounts
        self.totalRefunds = totalRefunds
        self.totalOrders = totalOrders
        self.totalVoids = totalVoids
        self.totalVoidValue = totalVoidValue
        self.chartData = chartData
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case totalGrossSales = "TotalGrossSales"
        case totalNetSales = "TotalNetSales"
        case totalTax = "TotalTax"
        case totalDiscounts = "TotalDiscounts"
        case totalRefunds = "TotalRefunds"
        case totalOrders = "TotalOrders"
        case totalVoids = "TotalVoids"
        case totalVoidValue = "TotalVoidValue"
        case chartData = "ChartData"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(totalGrossSales, forKey: .totalGrossSales)
        try container.encode(totalNetSales, forKey: .totalNetSales)
        try container.encode(totalTax, forKey: .totalTax)
        try container.encode(totalDiscounts, forKey: .totalDiscounts)
        try container.encode(totalRefunds, forKey: .totalRefunds)
        try container.encode(totalOrders, forKey: .totalOrders)
        try container.encode(totalVoids, forKey: .totalVoids)
        try container.encode(totalVoidValue, forKey: .totalVoidValue)
        try container.encode(chartData, forKey: .chartData)
    }
}

