//
// CreditCustomersWithSummaryResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct CreditCustomersWithSummaryResponse: Sendable, Codable, Hashable {

    public var summary: CreditCustomerSummary
    public var customers: [CreditCustomerResponse]
    public var page: Int
    public var pageSize: Int
    public var totalCount: Int
    public var totalPages: Int

    public init(summary: CreditCustomerSummary, customers: [CreditCustomerResponse], page: Int, pageSize: Int, totalCount: Int, totalPages: Int) {
        self.summary = summary
        self.customers = customers
        self.page = page
        self.pageSize = pageSize
        self.totalCount = totalCount
        self.totalPages = totalPages
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case summary = "Summary"
        case customers = "Customers"
        case page = "Page"
        case pageSize = "PageSize"
        case totalCount = "TotalCount"
        case totalPages = "TotalPages"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(summary, forKey: .summary)
        try container.encode(customers, forKey: .customers)
        try container.encode(page, forKey: .page)
        try container.encode(pageSize, forKey: .pageSize)
        try container.encode(totalCount, forKey: .totalCount)
        try container.encode(totalPages, forKey: .totalPages)
    }
}

