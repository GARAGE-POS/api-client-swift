//
// PurchaseOrderDetailRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PurchaseOrderDetailRequest: Sendable, Codable, Hashable {

    public static let itemIDRule = NumericRule<Int64>(minimum: 0, exclusiveMinimum: true, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let quantityRule = NumericRule<Double>(minimum: 0.0, exclusiveMinimum: true, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let costPerUnitRule = NumericRule<Double>(minimum: 0.0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let pricePerUnitRule = NumericRule<Double>(minimum: 0.0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public var itemID: Int64
    public var quantity: Double
    public var taxRate: Double?
    public var taxAmount: Double?
    public var unitID: Int64?
    public var discount: Double?
    public var costPerUnit: Double
    public var pricePerUnit: Double
    public var total: Double

    public init(itemID: Int64, quantity: Double, taxRate: Double? = nil, taxAmount: Double? = nil, unitID: Int64? = nil, discount: Double? = nil, costPerUnit: Double, pricePerUnit: Double, total: Double) {
        self.itemID = itemID
        self.quantity = quantity
        self.taxRate = taxRate
        self.taxAmount = taxAmount
        self.unitID = unitID
        self.discount = discount
        self.costPerUnit = costPerUnit
        self.pricePerUnit = pricePerUnit
        self.total = total
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case itemID = "ItemID"
        case quantity = "Quantity"
        case taxRate = "TaxRate"
        case taxAmount = "TaxAmount"
        case unitID = "UnitID"
        case discount = "Discount"
        case costPerUnit = "CostPerUnit"
        case pricePerUnit = "PricePerUnit"
        case total = "Total"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(itemID, forKey: .itemID)
        try container.encode(quantity, forKey: .quantity)
        try container.encodeIfPresent(taxRate, forKey: .taxRate)
        try container.encodeIfPresent(taxAmount, forKey: .taxAmount)
        try container.encodeIfPresent(unitID, forKey: .unitID)
        try container.encodeIfPresent(discount, forKey: .discount)
        try container.encode(costPerUnit, forKey: .costPerUnit)
        try container.encode(pricePerUnit, forKey: .pricePerUnit)
        try container.encode(total, forKey: .total)
    }
}

