//
// OverviewReportSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Summary/aggregate data for business overview report */
public struct OverviewReportSummary: Sendable, Codable, Hashable {

    public var totalSales: KpiMetric
    public var totalOrders: KpiMetric
    /** Net sales (total sales - refunds) */
    public var netSales: Double
    /** Total discounts given */
    public var totalDiscounts: Double
    /** Total refunds issued */
    public var totalRefunds: Double
    /** Average order value (net sales / total orders) */
    public var averageOrderValue: Double
    /** Total number of customers */
    public var totalCustomers: Int
    /** Number of new customers in the period */
    public var newCustomers: Int
    /** Number of returning customers in the period */
    public var returningCustomers: Int
    /** Number of inventory alerts (low stock items) */
    public var inventoryAlerts: Int
    /** Net sales per month for charting */
    public var netSalesPerMonth: [MonthlySalesData]
    /** Payment type breakdown for charting */
    public var paymentTypeBreakdown: [PaymentTypeData]
    /** Top selling products */
    public var topProducts: [TopProductData]

    public init(totalSales: KpiMetric, totalOrders: KpiMetric, netSales: Double, totalDiscounts: Double, totalRefunds: Double, averageOrderValue: Double, totalCustomers: Int, newCustomers: Int, returningCustomers: Int, inventoryAlerts: Int, netSalesPerMonth: [MonthlySalesData], paymentTypeBreakdown: [PaymentTypeData], topProducts: [TopProductData]) {
        self.totalSales = totalSales
        self.totalOrders = totalOrders
        self.netSales = netSales
        self.totalDiscounts = totalDiscounts
        self.totalRefunds = totalRefunds
        self.averageOrderValue = averageOrderValue
        self.totalCustomers = totalCustomers
        self.newCustomers = newCustomers
        self.returningCustomers = returningCustomers
        self.inventoryAlerts = inventoryAlerts
        self.netSalesPerMonth = netSalesPerMonth
        self.paymentTypeBreakdown = paymentTypeBreakdown
        self.topProducts = topProducts
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case totalSales = "TotalSales"
        case totalOrders = "TotalOrders"
        case netSales = "NetSales"
        case totalDiscounts = "TotalDiscounts"
        case totalRefunds = "TotalRefunds"
        case averageOrderValue = "AverageOrderValue"
        case totalCustomers = "TotalCustomers"
        case newCustomers = "NewCustomers"
        case returningCustomers = "ReturningCustomers"
        case inventoryAlerts = "InventoryAlerts"
        case netSalesPerMonth = "NetSalesPerMonth"
        case paymentTypeBreakdown = "PaymentTypeBreakdown"
        case topProducts = "TopProducts"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(totalSales, forKey: .totalSales)
        try container.encode(totalOrders, forKey: .totalOrders)
        try container.encode(netSales, forKey: .netSales)
        try container.encode(totalDiscounts, forKey: .totalDiscounts)
        try container.encode(totalRefunds, forKey: .totalRefunds)
        try container.encode(averageOrderValue, forKey: .averageOrderValue)
        try container.encode(totalCustomers, forKey: .totalCustomers)
        try container.encode(newCustomers, forKey: .newCustomers)
        try container.encode(returningCustomers, forKey: .returningCustomers)
        try container.encode(inventoryAlerts, forKey: .inventoryAlerts)
        try container.encode(netSalesPerMonth, forKey: .netSalesPerMonth)
        try container.encode(paymentTypeBreakdown, forKey: .paymentTypeBreakdown)
        try container.encode(topProducts, forKey: .topProducts)
    }
}

