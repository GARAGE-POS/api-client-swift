//
// SampleInvoicesStatus.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Sample invoices submission status. */
public struct SampleInvoicesStatus: Sendable, Codable, Hashable {

    /** Whether all samples are cleared. */
    public var allCleared: Bool
    /** Total number of samples (should be 6). */
    public var totalSamples: Int
    /** Number of cleared samples. */
    public var clearedSamples: Int
    /** Individual sample details. */
    public var samples: [SampleInvoiceInfo]

    public init(allCleared: Bool, totalSamples: Int, clearedSamples: Int, samples: [SampleInvoiceInfo]) {
        self.allCleared = allCleared
        self.totalSamples = totalSamples
        self.clearedSamples = clearedSamples
        self.samples = samples
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case allCleared = "AllCleared"
        case totalSamples = "TotalSamples"
        case clearedSamples = "ClearedSamples"
        case samples = "Samples"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(allCleared, forKey: .allCleared)
        try container.encode(totalSamples, forKey: .totalSamples)
        try container.encode(clearedSamples, forKey: .clearedSamples)
        try container.encode(samples, forKey: .samples)
    }
}

