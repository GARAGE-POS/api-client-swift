//
// GenerateUploadSasTokenResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Response containing the SAS token and upload instructions */
public struct GenerateUploadSasTokenResponse: Sendable, Codable, Hashable {

    /** The full SAS URL to use for uploading the file. Use this URL with HTTP PUT method to upload the file. */
    public var sasUrl: String
    /** The blob name (file path) in the storage container. This is the final path where the file will be stored. */
    public var blobName: String
    /** The container name where the file will be uploaded. */
    public var containerName: String
    /** The expiration time of the SAS token (UTC). Upload must be completed before this time. */
    public var expiresOn: Date
    /** The HTTP method to use for uploading the file. Always \"PUT\" for Azure Blob Storage. */
    public var uploadMethod: String
    /** Maximum allowed file size in bytes. */
    public var maxFileSizeBytes: Int64
    public var instructions: UploadInstructions

    public init(sasUrl: String, blobName: String, containerName: String, expiresOn: Date, uploadMethod: String, maxFileSizeBytes: Int64, instructions: UploadInstructions) {
        self.sasUrl = sasUrl
        self.blobName = blobName
        self.containerName = containerName
        self.expiresOn = expiresOn
        self.uploadMethod = uploadMethod
        self.maxFileSizeBytes = maxFileSizeBytes
        self.instructions = instructions
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case sasUrl = "SasUrl"
        case blobName = "BlobName"
        case containerName = "ContainerName"
        case expiresOn = "ExpiresOn"
        case uploadMethod = "UploadMethod"
        case maxFileSizeBytes = "MaxFileSizeBytes"
        case instructions = "Instructions"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(sasUrl, forKey: .sasUrl)
        try container.encode(blobName, forKey: .blobName)
        try container.encode(containerName, forKey: .containerName)
        try container.encode(expiresOn, forKey: .expiresOn)
        try container.encode(uploadMethod, forKey: .uploadMethod)
        try container.encode(maxFileSizeBytes, forKey: .maxFileSizeBytes)
        try container.encode(instructions, forKey: .instructions)
    }
}

