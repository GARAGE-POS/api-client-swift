//
// QuotationLineItemRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct QuotationLineItemRequest: Sendable, Codable, Hashable {

    public static let quantityRule = NumericRule<Double>(minimum: 0.0, exclusiveMinimum: true, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let unitPriceRule = NumericRule<Double>(minimum: 0.0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let grandTotalRule = NumericRule<Double>(minimum: 0.0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let subtotalRule = NumericRule<Double>(minimum: 0.0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public var itemID: Int64?
    public var packageID: Int64?
    public var notes: String?
    public var quantity: Double
    public var unitPrice: Double
    public var discountAmount: Double?
    public var discountPercent: Double?
    public var taxPercent: Double
    public var taxAmount: Double
    public var grandTotal: Double
    public var orderDiscountAllocation: Double
    public var subtotal: Double
    public var isInclusiveVAT: Bool

    public init(itemID: Int64? = nil, packageID: Int64? = nil, notes: String? = nil, quantity: Double, unitPrice: Double, discountAmount: Double? = nil, discountPercent: Double? = nil, taxPercent: Double, taxAmount: Double, grandTotal: Double, orderDiscountAllocation: Double, subtotal: Double, isInclusiveVAT: Bool) {
        self.itemID = itemID
        self.packageID = packageID
        self.notes = notes
        self.quantity = quantity
        self.unitPrice = unitPrice
        self.discountAmount = discountAmount
        self.discountPercent = discountPercent
        self.taxPercent = taxPercent
        self.taxAmount = taxAmount
        self.grandTotal = grandTotal
        self.orderDiscountAllocation = orderDiscountAllocation
        self.subtotal = subtotal
        self.isInclusiveVAT = isInclusiveVAT
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case itemID = "ItemID"
        case packageID = "PackageID"
        case notes = "Notes"
        case quantity = "Quantity"
        case unitPrice = "UnitPrice"
        case discountAmount = "DiscountAmount"
        case discountPercent = "DiscountPercent"
        case taxPercent = "TaxPercent"
        case taxAmount = "TaxAmount"
        case grandTotal = "GrandTotal"
        case orderDiscountAllocation = "OrderDiscountAllocation"
        case subtotal = "Subtotal"
        case isInclusiveVAT = "IsInclusiveVAT"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(itemID, forKey: .itemID)
        try container.encodeIfPresent(packageID, forKey: .packageID)
        try container.encodeIfPresent(notes, forKey: .notes)
        try container.encode(quantity, forKey: .quantity)
        try container.encode(unitPrice, forKey: .unitPrice)
        try container.encodeIfPresent(discountAmount, forKey: .discountAmount)
        try container.encodeIfPresent(discountPercent, forKey: .discountPercent)
        try container.encode(taxPercent, forKey: .taxPercent)
        try container.encode(taxAmount, forKey: .taxAmount)
        try container.encode(grandTotal, forKey: .grandTotal)
        try container.encode(orderDiscountAllocation, forKey: .orderDiscountAllocation)
        try container.encode(subtotal, forKey: .subtotal)
        try container.encode(isInclusiveVAT, forKey: .isInclusiveVAT)
    }
}

