//
// DashboardSummaryResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Complete dashboard summary response */
public struct DashboardSummaryResponse: Sendable, Codable, Hashable {

    /** Products with low stock relative to reorder level */
    public var stockAlerts: [StockAlertItem]
    /** Refund reasons with amounts */
    public var refundReasons: [RefundReasonItem]
    /** Discount usage summary */
    public var discounts: [DiscountUsageItem]
    /** Top selling products/services */
    public var popularSales: [PopularSaleItem]
    /** Employee performance summary */
    public var employeeTargets: [EmployeeTargetItem]

    public init(stockAlerts: [StockAlertItem], refundReasons: [RefundReasonItem], discounts: [DiscountUsageItem], popularSales: [PopularSaleItem], employeeTargets: [EmployeeTargetItem]) {
        self.stockAlerts = stockAlerts
        self.refundReasons = refundReasons
        self.discounts = discounts
        self.popularSales = popularSales
        self.employeeTargets = employeeTargets
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case stockAlerts = "StockAlerts"
        case refundReasons = "RefundReasons"
        case discounts = "Discounts"
        case popularSales = "PopularSales"
        case employeeTargets = "EmployeeTargets"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(stockAlerts, forKey: .stockAlerts)
        try container.encode(refundReasons, forKey: .refundReasons)
        try container.encode(discounts, forKey: .discounts)
        try container.encode(popularSales, forKey: .popularSales)
        try container.encode(employeeTargets, forKey: .employeeTargets)
    }
}

