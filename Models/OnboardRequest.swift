//
// OnboardRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Request for onboarding a terminal with ZATCA. No OTP required — uses the system compliance certificate directly. */
public struct OnboardRequest: Sendable, Codable, Hashable {

    public static let terminalIDRule = NumericRule<Int64>(minimum: 0, exclusiveMinimum: true, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    /** The Terminal ID to onboard. */
    public var terminalID: Int64

    public init(terminalID: Int64) {
        self.terminalID = terminalID
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case terminalID = "TerminalID"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(terminalID, forKey: .terminalID)
    }
}

