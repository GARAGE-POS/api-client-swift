//
// PackageReportSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Summary/aggregate data for package report */
public struct PackageReportSummary: Sendable, Codable, Hashable {

    /** Total number of unique packages */
    public var totalPackages: Int
    /** Total packages sold (sum of all quantities minus refunds) */
    public var totalPackagesSold: Double
    /** Total gross sales */
    public var totalGrossSales: Double
    /** Total net sales */
    public var totalNetSales: Double
    /** Total refunds */
    public var totalRefunds: Double
    /** Total discounts */
    public var totalDiscounts: Double
    /** Total taxes */
    public var totalTaxes: Double
    /** Total profit across all packages */
    public var totalProfit: Double
    /** Average profit margin percentage */
    public var averageProfitMargin: Double

    public init(totalPackages: Int, totalPackagesSold: Double, totalGrossSales: Double, totalNetSales: Double, totalRefunds: Double, totalDiscounts: Double, totalTaxes: Double, totalProfit: Double, averageProfitMargin: Double) {
        self.totalPackages = totalPackages
        self.totalPackagesSold = totalPackagesSold
        self.totalGrossSales = totalGrossSales
        self.totalNetSales = totalNetSales
        self.totalRefunds = totalRefunds
        self.totalDiscounts = totalDiscounts
        self.totalTaxes = totalTaxes
        self.totalProfit = totalProfit
        self.averageProfitMargin = averageProfitMargin
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case totalPackages = "TotalPackages"
        case totalPackagesSold = "TotalPackagesSold"
        case totalGrossSales = "TotalGrossSales"
        case totalNetSales = "TotalNetSales"
        case totalRefunds = "TotalRefunds"
        case totalDiscounts = "TotalDiscounts"
        case totalTaxes = "TotalTaxes"
        case totalProfit = "TotalProfit"
        case averageProfitMargin = "AverageProfitMargin"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(totalPackages, forKey: .totalPackages)
        try container.encode(totalPackagesSold, forKey: .totalPackagesSold)
        try container.encode(totalGrossSales, forKey: .totalGrossSales)
        try container.encode(totalNetSales, forKey: .totalNetSales)
        try container.encode(totalRefunds, forKey: .totalRefunds)
        try container.encode(totalDiscounts, forKey: .totalDiscounts)
        try container.encode(totalTaxes, forKey: .totalTaxes)
        try container.encode(totalProfit, forKey: .totalProfit)
        try container.encode(averageProfitMargin, forKey: .averageProfitMargin)
    }
}

