//
// OrderReportDto.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Individual order row in the report */
public struct OrderReportDto: Sendable, Codable, Hashable {

    /** Order ID (internal use for enrichment) */
    public var orderID: Int64
    /** Order number */
    public var orderNo: Int64
    /** Customer phone number */
    public var phoneNumber: String?
    /** Vehicle model name */
    public var carModel: String?
    /** Vehicle registration/plate number */
    public var plateNo: String?
    /** Order date/time */
    public var orderDate: Date
    /** Order subtotal (before discounts and tax) */
    public var subtotal: Double
    /** Discount amount applied */
    public var discount: Double
    /** Tax amount */
    public var tax: Double
    /** Refund amount */
    public var refund: Double
    /** Total amount (final price) */
    public var total: Double
    /** Order service status */
    public var orderStatus: Int
    /** Payment status */
    public var paymentStatus: Int

    public init(orderID: Int64, orderNo: Int64, phoneNumber: String? = nil, carModel: String? = nil, plateNo: String? = nil, orderDate: Date, subtotal: Double, discount: Double, tax: Double, refund: Double, total: Double, orderStatus: Int, paymentStatus: Int) {
        self.orderID = orderID
        self.orderNo = orderNo
        self.phoneNumber = phoneNumber
        self.carModel = carModel
        self.plateNo = plateNo
        self.orderDate = orderDate
        self.subtotal = subtotal
        self.discount = discount
        self.tax = tax
        self.refund = refund
        self.total = total
        self.orderStatus = orderStatus
        self.paymentStatus = paymentStatus
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case orderID = "OrderID"
        case orderNo = "OrderNo"
        case phoneNumber = "PhoneNumber"
        case carModel = "CarModel"
        case plateNo = "PlateNo"
        case orderDate = "OrderDate"
        case subtotal = "Subtotal"
        case discount = "Discount"
        case tax = "Tax"
        case refund = "Refund"
        case total = "Total"
        case orderStatus = "OrderStatus"
        case paymentStatus = "PaymentStatus"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(orderID, forKey: .orderID)
        try container.encode(orderNo, forKey: .orderNo)
        try container.encodeIfPresent(phoneNumber, forKey: .phoneNumber)
        try container.encodeIfPresent(carModel, forKey: .carModel)
        try container.encodeIfPresent(plateNo, forKey: .plateNo)
        try container.encode(orderDate, forKey: .orderDate)
        try container.encode(subtotal, forKey: .subtotal)
        try container.encode(discount, forKey: .discount)
        try container.encode(tax, forKey: .tax)
        try container.encode(refund, forKey: .refund)
        try container.encode(total, forKey: .total)
        try container.encode(orderStatus, forKey: .orderStatus)
        try container.encode(paymentStatus, forKey: .paymentStatus)
    }
}

